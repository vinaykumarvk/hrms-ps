# CLAUDE.md

Guidance for the AI agent working in this repository. This file is loaded into context on **every** session — it is the cheapest, always-on build-time layer of the pipeline's enforcement model (prevention → detection → enforcement). Keep it accurate.

> Generated by `new-project-setup`. Replace every `{{placeholder}}` and delete guidance that does not apply.

---

## ⚡ Golden rules (read first)

1. **Database change policy: {{db_change_policy}}.** {{db_policy_one_liner}}
2. **DB queries live in `{{db_access_dir}}`**, business logic in `{{business_logic_dir}}`. Do not inline DB access in routes/controllers.
3. **Use path aliases** ({{path_aliases}}), never deep relative paths across packages.
4. **Secrets only via environment variables** — never hardcode secrets, tokens, or keys; never commit `.env`. (Enforced by the `secrets_guard` hook.)
5. **Announce before destructive DB changes** — any DROP / ALTER / large UPDATE, or anything touching {{sensitive_tables}}, must be announced in `{{announce_channel}}` and approved first. (Enforced by the `db_change_guard` hook.)
6. **Every change traces to a requirement.** No code without an FR; no FR without a test.

---

## Project summary

- **Name:** {{project_name}}
- **What it is:** {{one_line_description}}
- **Type:** {{greenfield_or_brownfield}}

## Stack

- **Runtime / API:** {{runtime}} · {{api_style}} {{api_port}}
- **Frontend:** {{frontend}}
- **Database:** {{database}}
- **Auth:** {{auth}}
- **Deployment:** {{deployment}}
- **Observability:** {{observability}}

## Layering & conventions

- Data access → `{{db_access_dir}}`
- Business logic → `{{business_logic_dir}}`
- Path aliases → {{path_aliases}}
- {{other_conventions}}

## Database change policy — {{db_change_policy}}

{{db_policy_detail}}

- Schema source of truth: `{{schema_file}}`
- Verify schema with: `{{introspect_cmd}}`
- Sensitive tables (extra approval): {{sensitive_tables}}
- Forbidden commands: {{forbidden_commands}}

> The `db_change_guard` hook reads `project.config.yaml` and enforces this policy deterministically (block-with-override).

## Common commands

```bash
{{dev_cmd}}        # dev server
{{build_cmd}}      # production build
{{test_cmd}}       # run tests
{{typecheck_cmd}}  # type check
```

## Coordination

- Announce destructive/sensitive DB changes in: `{{announce_channel}}`
- Announce for: {{announce_for}}

---

## How this project works with the AI Dev Pipeline

This project is wired for the autonomous, traceability-first lifecycle. Run `/feature-life-cycle "<feature or path to a requirements doc>"` to take a feature from idea to reviewed, integrated code. The orchestrator reads this file and `project.config.yaml` for the rules above; the guard hooks enforce the binding ones; the gates and reviews handle the judgment-based ones.
