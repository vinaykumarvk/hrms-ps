# Approved DB changes
#
# The db_change_guard hook blocks DDL by default under the `migrations` change policy
# (project.config.yaml). A change is approved by adding a line here that appears in the blocked
# content — normally the migration filename — together with why it was approved and by whom.
#
# This file is an audit record, not a bypass. Never add a wildcard, and never approve a change
# that has not been announced. The secrets guard is never bypassed by this file.
#
# ---------------------------------------------------------------------------------------------
# 2026-07-26 · CC-021 · approved by: repository owner (instruction: "fix CC-019 and CC-021")
#
# 0034_ps13_clearance_unique_active.sql
#   Adds a PARTIAL UNIQUE INDEX (ck_clearance_unique_active) to security_clearances, enforcing at
#   most one ACTIVE, non-deleted clearance per (tenant_id, principal_type, principal_ref,
#   clearance_level).
#
#   Additive and forward-only: creates an index, no table/column/type is altered or removed, and
#   no data is rewritten. security_clearances is not in project.config.yaml sensitive_tables.
#
#   Why it needed approval: the migration and its compensating statement reference DROP INDEX,
#   which the guard treats as destructive DDL regardless of context.
#
#   Pre-existing duplicate ACTIVE rows would make this index fail to build. Verify before
#   applying — see docs/evidence/cc-021/duplicate-active-clearance-precheck.sql.
#
#   Compensating (rollback) statement recorded before application:
#     docs/evidence/cc-021/0034-compensating.sql
# ---------------------------------------------------------------------------------------------
0034_ps13_clearance_unique_active.sql
docs/evidence/cc-021/0034-compensating.sql
docs/evidence/cc-021/duplicate-active-clearance-precheck.sql

# ---------------------------------------------------------------------------------------------
# 2026-07-26 · W1 Gap A · approved by: agent under the standing autonomous-execution instruction
#
# 0035_w1_config_master_data.sql
#   Creates ten configuration tables the W1 Org-Admin screens administer but which had no
#   definition anywhere: business_units, devices, ip_allowlist, tenant_settings, integrations,
#   sso_providers, service_catalog_items, kb_articles, separation_policies, separation_workflows.
#
#   Additive and forward-only: CREATE TABLE / CREATE INDEX only. No existing object is altered
#   or removed and no data is rewritten. None of the ten is in project.config.yaml
#   sensitive_tables.
#
#   Derived from prototype screens rather than a signed FS — the source plan's W0 FS-gap list did
#   not include them (see docs/spec/full-coverage/w1-coverage.md). Assumptions are recorded in the
#   migration header. Credential material is referenced, never stored.
#
#   Compensating statement: docs/evidence/w1/0035-compensating.sql
# ---------------------------------------------------------------------------------------------
0035_w1_config_master_data.sql
docs/evidence/w1/0035-compensating.sql

# ---------------------------------------------------------------------------------------------
# 2026-07-26 · W2 Gap A · approved by: agent under the standing autonomous-execution instruction
#
# 0036_w2_leave_attendance_config.sql
#   Creates comp_off_rules, blackout_periods, decision_matrix. Unlike W1's Gap A these are NOT
#   inferred: every column traces to a field in the DwnB form-field exports shipped with the FS
#   (Tenant_Leaves_Compoff_Export.csv, Leave-Policy-Block-Leave-Export.csv, Approvalflows-Export.csv).
#   cfg-infraction is deliberately EXCLUDED — no field export specifies it.
#
#   Additive and forward-only. None of the three is in project.config.yaml sensitive_tables.
#   Compensating statement: docs/evidence/w2/0036-compensating.sql
# ---------------------------------------------------------------------------------------------
0036_w2_leave_attendance_config.sql
docs/evidence/w2/0036-compensating.sql
